'use strict';

describe('myApp.games module', function() {

  beforeEach(module('myApp.games'));

  describe('games controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var gamesCtrl = $controller('gamesCtrl');
      expect(gamesCtrl).toBeDefined();
    }));

  });
});
